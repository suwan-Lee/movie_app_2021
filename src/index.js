import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
    <App />,
  document.getElementById('root')
);

// 여기서 app은 component라고 부른다., 이것은 기본적으로 html을 반환하는 함수이다. componentt and function은 세트, 이런것을 jsx라고 부른다. 함수와 html의 조합이다. 유일한 react의 개념? 다른것은 다 js이다. 
// react는 하나의 component만을 rending한다. 여기서는 하나의 component가 App이 된다. 